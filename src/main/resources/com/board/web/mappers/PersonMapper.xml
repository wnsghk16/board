<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.web.mappers.PersonMapper">
    <select id="truncatePerson" parameterType="hashMap">
    	${TRUNCATE_PERSONS}
    </select>
    <select id="createDB" parameterType="hashMap">
  		${CREATE_DB}
  	</select>
  	 <select id="selectByUseridAndPasswd" resultType="com.board.web.domains.Person" parameterType="com.board.web.domains.Person">
  		
  	</select>
</mapper>